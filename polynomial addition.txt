#include<stdio.h> #include<conio.h> 
 
 void main() {     int co1[10],co2[10],ex1[10],ex2[10],cor[10],exr[10],m,n,i,j,k;     clrscr();     printf("\nEnter the no. of terms of first polynomial:");    scanf("%d",&m);    printf("\nEnter the no. of terms of second polynomial:");    scanf("%d",&n);    printf("\nEnter the coefficient and exponent of first polynomial: ");    for(i=0;i<m;i++)    scanf("%d%d",&co1[i],&ex1[i]);    printf("\nEnter the coefficient and exponent of second polynomial: ");    for(i=0;i<n;i++)    scanf("%d%d",&co2[i],&ex2[i]);    i=0;j=0;k=0;    while(i<m && j<n)    {        if(ex1[i]==ex2[i])        {            cor[k]=co1[i]+co2[j];            exr[k]=ex1[i];            i++;j++;k++;        }        else if(ex1[i]>ex2[i])        {            cor[k]=co1[i];            exr[k]=ex1[i];            i++;k++;        }        else        {            cor[k]=co2[j];            exr[k]=ex2[j];            j++;k++;        }           }     while(i<m)     {        cor[k]=co1[i];            exr[k]=ex1[i];            i++;k++;      }     while(j<n)     {            cor[k]=co2[j];            exr[k]=ex2[j];            j++;k++;       }     printf("The resultant is:");     for(i=0;i<k;i++)      printf("%dx^%d+",cor[i],exr[i]);    printf("\b=0");    getch(); }